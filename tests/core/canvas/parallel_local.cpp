#include <mln/core/canvas/parallel_local.hpp>

#include <mln/core/image/ndimage.hpp>
#include <mln/core/algorithm/equal.hpp>

#include <gtest/gtest.h>


TEST(Core, copy_buffer_into)
{
  //mln::image2d<uint8_t> ref      = {{0, 0, 0}, {0, 0, 0}, {0, 0, 0}};
  //int                   buffer[] = {1, 2, 3, 4, 5, 6, 7, 8, 9};
  //mln::TileExecutorBase::copy_buffer_into(buffer, ref);
  //ASSERT_TRUE(mln::equal(buffer, ref));
}
